services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: demo-web:1.0
    container_name: demo-web
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
